{
	"id": "ricorsione-e-algoritmi",
	"titolo": "Pattern di Ricorsione e Strutture Dati",
	"icona": "repeat",
	"descrizione": "Ricorsione strutturale, liste annidate, alberi, ricorsione in coda, accumulatori, insiemi e fold.",
	"items": [
		{
			"id": "ricorsione-rimuovi",
			"domanda": "Come si costruisce una funzione ricorsiva con parametri aggiuntivi (ad es. 'rimuovi')?",
			"risposta": "Si confronta l'elemento cercato con `(car l)`. Se coincidono, si restituisce `(cdr l)` ignorando la testa. Se non coincidono, si conserva `(car l)` in testa ricostruendo la lista con `cons` sulla chiamata ricorsiva a `(cdr l)`.",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Rimozione della prima occorrenza",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci rimuovi\n  (lambda (x l)\n    (cond\n      ((null? l) '())\n      ((equal? x (car l)) (cdr l))\n      (altrimenti (cons (car l)\n                        (rimuovi x (cdr l)))))))\n\n(rimuovi 2 '(1 2 3 2 4)) ; restituisce (1 3 2 4)"
				}
			],
			"tag": ["ricorsione", "liste", "cons"]
		},
		{
			"id": "ricorsione-rimuovi-tutti",
			"domanda": "Qual è la differenza tra rimuovere solo la prima occorrenza e rimuovere tutte le occorrenze ('rimuovi-tutti')?",
			"risposta": "Quando si trova l'elemento cercato, invece di restituire immediatamente `(cdr l)` interrompendo la ricerca, si continua a richiamare ricorsivamente la funzione su `(cdr l)` per eliminare anche le eventuali occorrenze successive.",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Rimozione di tutte le occorrenze",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci rimuovi-tutti\n  (lambda (x l)\n    (cond\n      ((null? l) '())\n      ((equal? x (car l)) (rimuovi-tutti x (cdr l)))\n      (altrimenti (cons (car l)\n                        (rimuovi-tutti x (cdr l)))))))\n\n(rimuovi-tutti 2 '(1 2 3 2 4 2)) ; restituisce (1 3 4)"
				}
			],
			"tag": ["ricorsione", "liste", "rimozione"]
		},
		{
			"id": "liste-annidate",
			"domanda": "Cosa cambia nella ricorsione quando una lista contiene al suo interno altre liste (liste annidate)?",
			"risposta": "Nelle liste piatte, `(car l)` è sempre un atomo. Nelle liste annidate, `(car l)` può essere esso stesso una lista. Pertanto, la ricorsione non deve avanzare solo lungo il `cdr`, ma deve poter esplorare anche la struttura interna del `car`.",
			"ebnf": "Datum            ::= Atomo | Lista | CoppiaPuntata | Abbreviazione",
			"esempio": [
				{
					"titolo": "Lista con liste annidate",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n'(a (b c) d) ; (car l) è l'atomo 'a, ma (car (cdr l)) è la lista '(b c)"
				}
			],
			"tag": ["liste-annidate", "alberi", "strutture"]
		},
		{
			"id": "primo-comandamento-liste-annidate",
			"domanda": "Come si estende il Primo Comandamento della ricorsione per gestire le liste annidate?",
			"risposta": "Per le liste annidate occorre porsi tre domande ordinate:\n1) La lista è vuota `(null? l)`?\n2) La testa `(car l)` è un atomo `(atomo? (car l))`?\n3) Altrimenti, la testa è una sottolista: occorre ricorrere sia su `(car l)` che su `(cdr l)`.",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Struttura di controllo per liste annidate",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n; Schema generale:\n; (cond\n;   ((null? l) <caso-base>)\n;   ((atomo? (car l)) <elabora-atomo-e-ricorri-su-cdr>)\n;   (altrimenti <ricorri-su-car-e-su-cdr>))"
				}
			],
			"tag": ["comandamenti", "pattern-ricorsivi", "cond"]
		},
		{
			"id": "ricorsione-su-alberi",
			"domanda": "Cos'è la ricorsione su alberi (o ricorsione doppia) e come si applica a car e cdr?",
			"risposta": "È un motivo ricorsivo in cui la funzione richiama se stessa due volte nello stesso ramo: una volta sul primo sotto-albero `(car l)` e una volta sul resto dell'albero `(cdr l)`, combinando poi i due risultati (ad esempio con `cons` o `+`).",
			"ebnf": "Applicazione     ::= \"(\" Espressione Espressione* \")\"",
			"esempio": [
				{
					"titolo": "Duplicazione ricorsiva di un albero",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci duplica-albero\n  (lambda (l)\n    (cond\n      ((null? l) '())\n      ((atomo? (car l)) (cons (car l) (duplica-albero (cdr l))))\n      (altrimenti (cons (duplica-albero (car l))\n                        (duplica-albero (cdr l)))))))\n\n(duplica-albero '(a (b c))) ; restituisce (a (b c))"
				}
			],
			"tag": ["alberi", "ricorsione-doppia", "cons"]
		},
		{
			"id": "conta-atomi-albero",
			"domanda": "Come si implementa il conteggio di tutti gli atomi in una lista annidata ('conta-atomi')?",
			"risposta": "Si applica la ricorsione su alberi: se la lista è vuota restituisce `0`; se il `car` è un atomo aggiunge `1` al conteggio del `cdr`; altrimenti somma il risultato di `conta-atomi` su `(car l)` con quello su `(cdr l)`.",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Conteggio ricorsivo su albero",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci conta-atomi\n  (lambda (l)\n    (cond\n      ((null? l) 0)\n      ((atomo? (car l)) (+ 1 (conta-atomi (cdr l))))\n      (altrimenti (+ (conta-atomi (car l))\n                     (conta-atomi (cdr l)))))))\n\n(conta-atomi '(a (b (c d)) e)) ; restituisce 5"
				}
			],
			"tag": ["alberi", "conteggio", "ricorsione"]
		},
		{
			"id": "appiattisci-liste",
			"domanda": "Come si trasforma una lista di liste annidate in una lista 'piatta' di soli atomi ('appiattisci')?",
			"risposta": "Si ricorre sia sul `car` che sul `cdr`. Se la testa `(car l)` è un atomo, la si aggiunge in testa all'appiattimento del `cdr`. Se la testa è una lista, si applica `appiattisci` sia al `car` che al `cdr` fondendo i risultati con `append` (o `concatena`).",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Appiattimento di liste annidate",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci appiattisci\n  (lambda (l)\n    (cond\n      ((null? l) '())\n      ((atomo? (car l)) (cons (car l) (appiattisci (cdr l))))\n      (altrimenti (append (appiattisci (car l))\n                          (appiattisci (cdr l)))))))\n\n(appiattisci '((a b) (c (d e)))) ; restituisce (a b c d e)"
				}
			],
			"tag": ["appiattisci", "flatten", "liste"]
		},
		{
			"id": "ricorsione-in-coda-tail-call",
			"domanda": "Cos'è la ricorsione in coda (tail recursion) e perché è diversa dalla ricorsione classica?",
			"risposta": "Nella ricorsione in coda, la chiamata ricorsiva è l'ULTIMA operazione eseguita nel corpo (nessuna operazione rimane in sospeso come `cons` o `+`). Ciò permette all'interprete di riutilizzare lo stesso frame di memoria senza accumulare lo stack di chiamate.",
			"ebnf": "Applicazione     ::= \"(\" Espressione Espressione* \")\"",
			"esempio": [
				{
					"titolo": "Ricorsione in coda con accumulatore",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci lunghezza-coda\n  (lambda (l acc)\n    (cond\n      ((null? l) acc)\n      (altrimenti (lunghezza-coda (cdr l) (+ acc 1))))))\n\n(lunghezza-coda '(10 20 30) 0) ; restituisce 3"
				}
			],
			"tag": ["tail-recursion", "accumulatore", "ottimizzazione"]
		},
		{
			"id": "inversione-accumulatore",
			"domanda": "Come si usa un accumulatore per invertire una lista in modo efficiente ('inverti')?",
			"risposta": "Con un accumulatore preleviamo `(car l)` e lo mettiamo in testa all'accumulatore con `(cons (car l) acc)`. Alla fine della lista, l'accumulatore conterrà tutti gli elementi nell'ordine inverso, in tempo lineare $O(n)$.",
			"ebnf": "Applicazione     ::= \"(\" Espressione Espressione* \")\"",
			"esempio": [
				{
					"titolo": "Inversione ricorsiva in coda",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci inverti-aux\n  (lambda (l acc)\n    (se (null? l)\n        acc\n        (inverti-aux (cdr l) (cons (car l) acc)))))\n\n(definisci inverti\n  (lambda (l)\n    (inverti-aux l '())))\n\n(inverti '(1 2 3 4)) ; restituisce (4 3 2 1)"
				}
			],
			"tag": ["inverti", "reverse", "accumulatore"]
		},
		{
			"id": "uguaglianza-profonda",
			"domanda": "Come si confrontano due strutture dati arbitrarie per verificare se sono identiche ('uguale?')?",
			"risposta": "Il confronto ricorsivo profondo verifica se: 1) Sono entrambi atomi uguali con `eq?`, 2) Sono entrambe liste vuote, oppure 3) Hanno sia i rispettivi `car` uguali che i rispettivi `cdr` uguali (ricorsione doppia strutturale).",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Uguaglianza strutturale profonda",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci uguale?\n  (lambda (d1 d2)\n    (cond\n      ((e (atomo? d1) (atomo? d2)) (eq? d1 d2))\n      ((o (atomo? d1) (atomo? d2)) #falso)\n      ((e (null? d1) (null? d2)) #vero)\n      ((o (null? d1) (null? d2)) #falso)\n      (altrimenti (e (uguale? (car d1) (car d2))\n                     (uguale? (cdr d1) (cdr d2)))))))\n\n(uguale? '(1 (2 3)) '(1 (2 3))) ; restituisce #vero"
				}
			],
			"tag": ["uguale?", "deep-equal", "strutture"]
		},
		{
			"id": "piega-destra-foldr",
			"domanda": "Cos'è l'astrazione di ricorsione a destra ('piega-destra' o 'foldr')?",
			"risposta": "`piega-destra` generalizza lo schema della ricorsione strutturale sulle liste: sostituisce la lista vuota `'()` con un valore iniziale *base*, e sostituisce il costruttore `cons` con una funzione binaria `f` passata come argomento.",
			"ebnf": "Lambda           ::= \"(\" \"lambda\" Parametri Corpo \")\"",
			"esempio": [
				{
					"titolo": "Incapsulare la ricorsione con piega-destra",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci piega-destra\n  (lambda (f base l)\n    (cond\n      ((null? l) base)\n      (altrimenti (f (car l)\n                     (piega-destra f base (cdr l)))))))\n\n; Sommare gli elementi di una lista\n(piega-destra + 0 '(1 2 3 4)) ; restituisce 10"
				}
			],
			"tag": ["piega-destra", "foldr", "astrazioni"]
		},
		{
			"id": "piega-sinistra-foldl",
			"domanda": "Qual è la differenza tra 'piega-destra' e 'piega-sinistra' ('foldl')?",
			"risposta": "`piega-destra` elabora la lista partendo dal fondo verso la testa (accumulando i risultati al rientro dallo stack), mentre `piega-sinistra` elabora gli elementi da sinistra a destra con un accumulatore ed è naturalmente ricorsiva in coda.",
			"ebnf": "Applicazione     ::= \"(\" Espressione Espressione* \")\"",
			"esempio": [
				{
					"titolo": "Piega a sinistra ricorsiva in coda",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci piega-sinistra\n  (lambda (f acc l)\n    (cond\n      ((null? l) acc)\n      (altrimenti (piega-sinistra f\n                                  (f acc (car l))\n                                  (cdr l))))))\n\n(piega-sinistra + 0 '(1 2 3 4)) ; restituisce 10"
				}
			],
			"tag": ["piega-sinistra", "foldl", "accumulatore"]
		},
		{
			"id": "insiemi-predicato",
			"domanda": "Come si verifica ricorsivamente se una lista rappresenta un Insieme (senza duplicati)?",
			"risposta": "Un insieme non deve contenere elementi ripetuti. Ricorsivamente: una lista vuota è un insieme; se `(car l)` appartiene al resto della lista `(cdr l)` allora NON è un insieme; altrimenti si controlla se il `(cdr l)` è un insieme.",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Predicato insieme?",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci appartiene?\n  (lambda (x l)\n    (cond\n      ((null? l) #falso)\n      ((equal? x (car l)) #vero)\n      (altrimenti (appartiene? x (cdr l))))))\n\n(definisci insieme?\n  (lambda (l)\n    (cond\n      ((null? l) #vero)\n      ((appartiene? (car l) (cdr l)) #falso)\n      (altrimenti (insieme? (cdr l))))))\n\n(insieme? '(1 2 3)) ; restituisce #vero\n(insieme? '(1 2 1)) ; restituisce #falso"
				}
			],
			"tag": ["insiemi", "set", "predicati"]
		},
		{
			"id": "intersezione-insiemi",
			"domanda": "Come si calcola l'Intersezione tra due insiemi tramite ricorsione?",
			"risposta": "Si esamina ogni elemento del primo insieme: se fa parte anche del secondo insieme (tramite `appartiene?`), lo si mantiene nel risultato con `cons`; altrimenti lo si scarta, proseguendo ricorsivamente sul resto del primo insieme.",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Intersezione di due insiemi",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci intersezione\n  (lambda (s1 s2)\n    (cond\n      ((null? s1) '())\n      ((appartiene? (car s1) s2)\n       (cons (car s1) (intersezione (cdr s1) s2)))\n      (altrimenti (intersezione (cdr s1) s2)))))\n\n(intersezione '(1 2 3) '(2 3 4)) ; restituisce (2 3)"
				}
			],
			"tag": ["intersezione", "insiemi", "ricorsione"]
		},
		{
			"id": "sottoinsieme",
			"domanda": "Come si verifica se un insieme è un sottoinsieme di un altro ('sottoinsieme?')?",
			"risposta": "Si usa la ricorsione strutturale sul primo insieme: per ogni suo elemento `(car s1)`, si controlla se appartiene anche al secondo insieme `s2`. Se la verifica passa per tutti gli elementi e si raggiunge il caso base `(null? s1)`, allora `s1` è un sottoinsieme di `s2`.",
			"ebnf": "Cond             ::= \"(\" \"cond\" ClausolaCond+ \")\"",
			"esempio": [
				{
					"titolo": "Verifica di sottoinsieme",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci sottoinsieme?\n  (lambda (s1 s2)\n    (cond\n      ((null? s1) #vero)\n      ((appartiene? (car s1) s2) (sottoinsieme? (cdr s1) s2))\n      (altrimenti #falso))))\n\n(sottoinsieme? '(2 3) '(1 2 3 4)) ; restituisce #vero"
				}
			],
			"tag": ["sottoinsieme", "insiemi", "predicati"]
		},
		{
			"id": "lista-associazioni-alist",
			"domanda": "Cos'è una Lista di Associazioni (A-list) e come si usa per rappresentare un ambiente?",
			"risposta": "Una A-list è una lista di coppie `((chiave . valore) ...)` dove ogni coppia associa un nome a un dato. È la struttura fondamentale usata dagli interpreti per rappresentare dizionari, frame di memoria e tabelle di simboli.",
			"ebnf": "CoppiaPuntata    ::= \"(\" Datum+ \".\" Datum \")\"",
			"esempio": [
				{
					"titolo": "Ricerca in una lista di associazioni",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n; Ambiente rappresentato come: '((x . 10) (y . 20))\n(definisci cerca-env\n  (lambda (var env)\n    (cond\n      ((null? env) #falso)\n      ((eq? var (car (car env))) (cdr (car env)))\n      (altrimenti (cerca-env var (cdr env))))))\n\n(cerca-env 'y '((x . 10) (y . 20))) ; restituisce 20"
				}
			],
			"tag": ["a-list", "dizionari", "ambiente"]
		},
		{
			"id": "valutatore-espressioni-aritmetiche",
			"domanda": "Come si usa la ricorsione su alberi per valutare un'espressione aritmetica in formato prefisso?",
			"risposta": "Se l'espressione è un numero (atomo), il suo valore è il numero stesso. Altrimenti è una lista il cui primo elemento è l'operatore e i successivi sono i sotto-alberi degli operandi. Si valutano ricorsivamente gli operandi e si applica l'operatore.",
			"ebnf": "Datum            ::= Atomo | Lista | CoppiaPuntata | Abbreviazione",
			"esempio": [
				{
					"titolo": "Valutatore di espressioni aritmetiche",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci valuta-arit\n  (lambda (e)\n    (cond\n      ((atomo? e) e)\n      ((eq? (car e) '+) (+ (valuta-arit (car (cdr e)))\n                           (valuta-arit (car (cdr (cdr e))))))\n      ((eq? (car e) '*) (* (valuta-arit (car (cdr e)))\n                           (valuta-arit (car (cdr (cdr e)))))))))\n\n(valuta-arit '(+ (* 2 3) 4)) ; restituisce 10"
				}
			],
			"tag": ["interprete-aritmetico", "alberi", "espressioni"]
		},
		{
			"id": "continuation-passing-style-cps",
			"domanda": "Cos'è lo Stile a Passaggio di Continuazione (CPS - Continuation-Passing Style)?",
			"risposta": "È una tecnica in cui le funzioni non restituiscono direttamente un valore, ma ricevono un parametro extra: la *continuazione* `k` (una funzione lambda). Quando il risultato è calcolato, viene passato come argomento a `k`, trasformando qualsiasi ricorsione complessa in ricorsione in coda.",
			"ebnf": "Applicazione     ::= \"(\" Espressione Espressione* \")\"",
			"esempio": [
				{
					"titolo": "Fattoriale in Continuation-Passing Style",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n(definisci fattoriale-cps\n  (lambda (n k)\n    (se (zero? n)\n        (k 1)\n        (fattoriale-cps (p n)\n                        (lambda (res) (k (* n res)))))))\n\n(fattoriale-cps 5 (lambda (x) x)) ; restituisce 120"
				}
			],
			"tag": ["cps", "continuazioni", "funzionale-avanzato"]
		},
		{
			"id": "regola-oro-astrazioni",
			"domanda": "Qual è la regola d'oro per evitare di riscrivere la ricorsione da zero?",
			"risposta": "Identifica la natura dell'operazione:\n- Per trasformare ogni elemento usa `mappa`.\n- Per selezionare o filtrare usa `filtra`.\n- Per accumulare o combinare usa `piega-destra` o `piega-sinistra`.\nComporre funzioni di ordine superiore rende il codice conciso, leggibile e privo di errori sui casi base.",
			"ebnf": "Programma        ::= Forma*",
			"esempio": [
				{
					"titolo": "Composizione pulita mediante astrazioni",
					"tipo": "codice",
					"contenuto": "; ambiente: standard\n; Somma dei quadrati dei soli numeri pari in una lista:\n(definisci somma-quadrati-pari\n  (lambda (l)\n    (piega-destra + 0\n      (mappa (lambda (x) (* x x))\n             (filtra pari? l)))))\n\n(somma-quadrati-pari '(1 2 3 4)) ; 2^2 + 4^2 = 20"
				}
			],
			"tag": ["astrazioni", "best-practices", "composizione"]
		}
	]
}
